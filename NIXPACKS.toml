# Caminho: NIXPACKS.toml - Frontend Configuration

[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "npm run build:hmg && npx vite preview --host 0.0.0.0 --port $PORT" 